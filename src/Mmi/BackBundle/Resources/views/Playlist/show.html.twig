{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Playlist</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Plnom</th>
                <td>{{ entity.plNom }}</td>
            </tr>
            <tr>
                <th>Plduree</th>
                <td>{{ entity.plDuree }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('playlist') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('playlist_edit', { 'idp': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
